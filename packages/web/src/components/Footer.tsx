const Footer = () => {
  return (
    <footer className="border-t border-border px-4 py-6 text-center text-xs text-muted sm:px-6">
      <p>
        CineWeb is a community project — movie similarities are asserted and voted on by users, not
        an algorithm.
      </p>
      <p className="mt-1">This product uses the TMDB API but is not endorsed or certified by TMDB.</p>
    </footer>
  );
};

export default Footer;
