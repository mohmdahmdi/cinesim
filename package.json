{
  "dependencies": {
    "@nestjs/mapped-types": "*"
  },
  "name": "movie-recom",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:api": "yarn workspace api start:dev",
    "dev:web": "yarn workspace web dev",
    "dev": "concurrently \"yarn dev:api\" \"yarn dev:web\"",
    "db:create": "yarn workspace api create-db",
    "db:seed": "yarn workspace api seed"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
